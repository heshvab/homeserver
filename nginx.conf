# -- Deny unknown hosts --

server {
    listen 443 ssl default_server;
    server_name _;

    ssl_certificate     /etc/letsencrypt/live/shvab.xyz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/shvab.xyz/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    return 444;
}


# -- Pihole --

# -- For Local Access --
server {
    listen 84;
    server_name _;

    client_max_body_size 1G;
    fastcgi_read_timeout 3600s;
    client_body_buffer_size 5M;

    location / {
        proxy_pass http://pihole-server:80;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# -- For Remote Access --
server {
    listen 443 ssl;
    server_name pihole.shvab.xyz;

    ssl_certificate     /etc/letsencrypt/live/pihole.shvab.xyz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/pihole.shvab.xyz/privkey.pem;

    client_max_body_size 1G;
    fastcgi_read_timeout 3600s;
    client_body_buffer_size 5M;

    location / {
        proxy_pass http://pihole-server:80;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}


# -- Immich --

# -- For Local Access --
server {
    listen 83;
    server_name _;

    client_max_body_size 1G;
    fastcgi_read_timeout 3600s;
    client_body_buffer_size 5M;

    location / {
        proxy_pass http://immich-server:2283;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# -- For Remote Access --
server {
    listen 443 ssl;
    server_name immich.shvab.xyz;

    ssl_certificate     /etc/letsencrypt/live/immich.shvab.xyz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/immich.shvab.xyz/privkey.pem;

    client_max_body_size 1G;
    fastcgi_read_timeout 3600s;
    client_body_buffer_size 5M;

    location / {
        proxy_pass http://immich-server:2283;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}


# -- Jellyfin --

# -- For Local Access -
server {
    listen 80;
    server_name _;

    client_max_body_size 100M;
    fastcgi_read_timeout 3600s;
    client_body_buffer_size 5M;

    location / {
        proxy_pass http://jellyfin-server:8096;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";


        proxy_buffering off;
    }
}

# -- For Remote Access --
server {
    listen 443 ssl;
    server_name jellyfin.shvab.xyz;

    ssl_certificate     /etc/letsencrypt/live/jellyfin.shvab.xyz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/jellyfin.shvab.xyz/privkey.pem;

    client_max_body_size 100M;
    fastcgi_read_timeout 3600s;
    client_body_buffer_size 5M;

    location / {
        proxy_pass http://jellyfin-server:8096;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    

	proxy_buffering off;
    }
}


# -- Nextcloud --

# -- For Local Access --
server {
    listen 82;
    server_name nextcloud.shvab.xyz;

    client_max_body_size 20G;
    fastcgi_read_timeout 3600s;
    client_body_buffer_size 5M;
    client_body_temp_path /cache 1 2;

    location / {
        proxy_pass http://nextcloud-server:80;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_read_timeout 3600s;
        proxy_connect_timeout 3600s;
        proxy_send_timeout 3600s;

        proxy_request_buffering off;
        proxy_buffering off;
    }
}

# -- For Remote Access --
server {
    listen 443 ssl;
    server_name nextcloud.shvab.xyz;

    ssl_certificate     /etc/letsencrypt/live/nextcloud.shvab.xyz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/nextcloud.shvab.xyz/privkey.pem;

    client_max_body_size 20G;
    fastcgi_read_timeout 3600s;
    client_body_buffer_size 5M;
    client_body_temp_path /cache 1 2;

    location / {
        proxy_pass http://nextcloud-server:80;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

	proxy_read_timeout 3600s;
	proxy_connect_timeout 3600s;
	proxy_send_timeout 3600s;
    
	proxy_request_buffering off;
	proxy_buffering off;
    }
}


# -- Feishin --

# For Local Access --
server {
    listen 81;
    server_name _;

    client_max_body_size 100M;
    fastcgi_read_timeout 3600s;
    client_body_buffer_size 5M;

    location / {
        proxy_pass http://feishin-server:9180;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";


        proxy_buffering off;
    }
}

# -- For Remote Access --
server {
    listen 443 ssl;
    server_name feishin.shvab.xyz;

    ssl_certificate     /etc/letsencrypt/live/feishin.shvab.xyz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/feishin.shvab.xyz/privkey.pem;

    client_max_body_size 100M;
    fastcgi_read_timeout 3600s;
    client_body_buffer_size 5M;

    location / {
        proxy_pass http://feishin-server:9180;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";


        proxy_buffering off;
    }
}
